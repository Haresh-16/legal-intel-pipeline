{
  "name": "worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply legal-intel-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply legal-intel-db --remote",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "drizzle-orm": "^0.36.0",
    "zod": "^3.23.8",
    "openai": "^4.68.0",
    "ulid": "^2.3.0"
  },
  "devDependencies": {
    "drizzle-kit": "^0.28.0",
    "vitest": "^2.1.4",
    "wrangler": "^3.84.0",
    "@cloudflare/workers-types": "^4.20241022.0",
    "typescript": "^5.6.3"
  }
}
